# Pinned dependencies for the GR00T RL learner container.
# Aligned with i4h-workflows v0.5.0 (workflows/rheo/docker/Dockerfile.x86)

# PyTorch (installed separately via --index-url cu124)
# torch==2.6.0
# torchvision==0.21.0

# Ray (RLinf minimum requirement)
ray[default]==2.47.0

# RLinf dependencies
hydra-core==1.3.2
omegaconf==2.3.0
tensorboard>=2.14.0

# GR00T / Transformers (must stay on 4.x — AutoModelForVision2Seq removed in 5.x)
transformers==4.51.3
accelerate==1.2.1
safetensors>=0.4.0
huggingface-hub>=0.20.0
pydantic==2.10.6
typing_extensions==4.12.2

# Vision / data
albumentations==1.4.18
numpy==1.26.4
Pillow>=10.0.0
opencv-python-headless==4.11.0.86

# Utilities
PyYAML==6.0.2
Requests==2.32.3
tqdm==4.67.1
protobuf==3.20.3
einops==0.8.1

# AWS
awscli>=1.30.0
boto3>=1.30.0
